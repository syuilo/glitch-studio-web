import { defineEffect } from '../fx-definition.ts';

export default defineEffect({
	name: 'pointerTrail',
	displayName: 'pointerTrail',
	category: 'utility',
	paramDefs: {
	},
	getDefaultParams: () => ({
	}),
});
